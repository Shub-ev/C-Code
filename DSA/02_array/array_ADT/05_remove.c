#include "array_ADT.h"

void remove(struct Array* arr, int index)
{
    if(arr->length == 0)
    {
        printf("Array is empty!\n");
        return;
    }
    
    if(index >= arr->length)
    {
        
    }
}