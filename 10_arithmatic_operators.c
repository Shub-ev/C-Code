#include<stdio.h>

int main(){
    // ***** assignment operators *****
    // used to perform arithmatic operators over vars
    // + - / * % ++ --


    // ++x != x++
    // ++x -> first increment number then expression if
    // x++ -> first expression 

    // increment and decrement gererates more optimized/fast object file hence use --/++ 

    // ***** operator precedence *****

    // highest  ->          ()
    //                    ++ , -- 
    //                 -(unary minus)
    //                 * , / , %
    // lowest              +, -

    // on same level left to right
}